@extends('layout.master')@section('content')    <div class="container margin-top-20px">        <div class="row">            <div class="col-md-4">                @include('layout.partial.product-sidebar')            </div>            <div class="col-md-8">                <div class="widget">                    <h3> All Products</h3>                    @foreach( $products as $product)                        <div class="row">                        <div class="col-md-3">                            <div class="card" >                                @foreach( $product->images as $image)                                <img class="card-img-top featured-image" src="{{asset('image/products/'. $image->image)}}" alt="Card image">                                @endforeach                                <div class="card-body">                                    <h4 class="card-title">{{ $product->title }}</h4>                                    <p class="card-text"> {{ $product->price }}</p>                                    <p class="card-text"> {{ $product->description }}</p>                                    <a href="#" class="btn btn-outline-warning">Add Cart</a>                                </div>                            </div>                        </div>                    </div>                        @endforeach                </div>            </div>        </div>    </div>@endsection